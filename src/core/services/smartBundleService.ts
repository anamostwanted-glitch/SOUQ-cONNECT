import { GoogleGenAI, Type, Schema } from "@google/genai";
import { UserProfile, Category } from "../types";

export const analyzeEventBundle = async (query: string, categories: Category[]) => {
  // Logic here
}
